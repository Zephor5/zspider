现代化改造 TODO List
=====================

约定
----

- ``[x]`` 已完成
- ``[ ]`` 未完成
- ``[-]`` 当前阶段不做，后续再评估

大目标
------

- 把 ZSpider 维持为一套自托管应用，而不是面向 PyPI 的通用库
- 保留 ``web / dispatcher / crawler`` 三进程形态
- 保留任务、解析器、字段配置这套核心领域模型
- 逐步降低运行时老化、安全风险和维护摩擦

当前状态快照
------------

- [x] 文档重组为中英文双语结构
- [x] 项目定位收口为“自托管应用”
- [x] 本地入口统一到 ``make`` / ``python -m`` / ``docker compose``
- [x] 增加 ``settings`` 层并把核心运行时配置改为环境变量驱动
- [x] 区分 ``development / testing / production`` 环境
- [x] Web 进程改为 WSGI 方式启动，替换 ``app.run``
- [x] 增加 ``/healthz`` 和 ``/readyz`` 端点
- [x] 补充运维文档和 ``.env.example``
- [x] 增加显式管理员初始化命令 ``make bootstrap-admin``

P0：基线与工具链
----------------

- [x] 补齐开发与运行文档
- [x] 提供统一本地入口 ``make install`` / ``make dev`` / ``make docs``
- [ ] 明确仓库支持的 Python 版本策略
- [x] 给 CI 增加 lint 检查，而不只是跑单元测试
- [x] 引入 ``ruff``
- [ ] 引入 ``pytest`` 并逐步替代 ``unittest discover``
- [ ] 为关键模块增加最小 ``mypy`` 覆盖
- [ ] 补全 ``pyproject.toml``，让它真正承载工具链配置

P1：配置与部署治理
------------------

- [x] MQ、Memcached、MongoDB 地址改为环境变量驱动
- [x] 引入统一 ``settings`` 层
- [x] 区分开发、测试、生产环境
- [x] 用 ``docker-compose.services.yml`` 明确定义本地依赖
- [x] 用 WSGI 方式替换 Flask 开发式启动
- [x] 增加健康检查与 readiness 文档
- [x] 为 MongoDB / RabbitMQ 增加 Compose 健康检查
- [ ] 为 Memcached 增加更稳妥的 Compose 健康检查
- [ ] 为 Dispatcher 增加独立健康检查端点
- [ ] 为 Crawler 增加独立健康检查或存活状态上报
- [ ] 增加统一的部署文档，明确单机 / 容器运行方式
- [ ] 清理历史 ``supervisor`` 配置，决定是保留还是淘汰

P2：安全加固
------------

- [x] 移除“首次登录自动成为管理员”
- [x] 提供显式管理员初始化命令
- [x] 将密码哈希从 SHA-256 升级到 ``bcrypt`` 或 ``argon2``
- [x] 为密码哈希升级设计兼容迁移路径
- [x] 补充登录、登出、管理员初始化的回归测试
- [ ] 检查 ``SECRET_KEY``、session cookie、CSRF 配置
- [ ] 明确管理员操作的权限边界
- [x] 替换 ``publish.py`` 中的 ``eval``

P3：Web/API 边界重构
--------------------

- [ ] 把登录、任务管理、日志查看等逻辑逐步提取到 service 层
- [ ] 为任务、字段、结果、日志定义更稳定的内部 API
- [ ] 降低模板和数据库模型的直接耦合
- [ ] 把当前 HTML 后台逐步收口成“一个客户端”
- [ ] 为 Web 层增加更清晰的错误处理和 JSON 响应边界

P4：运行时与数据平面
--------------------

- [ ] 明确 Dispatcher -> Crawler 消息 schema
- [ ] 给任务执行补齐状态模型和运行历史
- [ ] 改进失败重试、死信或补偿机制
- [ ] 为发布失败、解析失败、抓取失败补充更清晰的状态码
- [ ] 评估是否用 Redis 替代 Memcached
- [ ] 增加队列堆积、抓取成功率、发布失败率等指标

P5：体验层现代化
----------------

- [ ] 重构后台信息架构
- [ ] 改进任务创建流程
- [ ] 增强解析调试与预览能力
- [ ] 增加运行历史、差异视图和结果检查能力
- [ ] 优化日志与失败反馈

明确非目标
----------

- [-] 不做整仓重写
- [-] 不先拆成微服务
- [-] 当前阶段不做前后端大拆分
- [-] 不为了“现代化外观”先重做前端
- [-] 不在没有运行痛点前先做数据库迁移
- [-] 不把“发 PyPI”当目标

建议的下一批任务
----------------

如果按当前状态继续推进，建议下一批实际工作就是这 5 项：

- [ ] 把密码哈希升级到 ``bcrypt`` 或 ``argon2``
- [ ] 为登录和管理员初始化补单元测试
- [ ] 替换 ``publish.py`` 里的 ``eval``
- [ ] 给 CI 增加 lint 阶段
- [x] 给 Dispatcher 增加健康检查或 readiness 端点
